<p>All classes available</p>
<ul>
  <%@lessons.each do |lesson|%>
  <li><%=lesson.name + " -" + lesson.address%></li><%= link_to "Edit this class", edit_user_lesson_path(lesson.user, lesson)%>
  <%end%>
</ul>
<%= link_to("Add a new class", new_user_lesson_path)%>
